# Server Configuration
PORT=3000

# Database Configuration
# Path to SQLite database file. Defaults to server/pulse.db
# In production, consider using a persistent volume or external database
DB_PATH=server/pulse.db
